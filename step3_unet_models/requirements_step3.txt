# Step 3: UNet Models Requirements
# Complete requirements for loading UNet models and IP-Adapter

# Core ML Libraries
torch>=2.0.0
torchvision>=0.15.0

# HuggingFace Libraries (for models)
transformers==4.35.0  # Pin to compatible version with huggingface_hub 0.16.x
diffusers>=0.24.0,<0.25.0  # Pin to compatible version
huggingface_hub>=0.16.0,<0.17.0  # Pin to version with cached_download

# Image Processing
Pillow>=10.0.0

# Scientific Computing
numpy>=1.24.0

# Tensor Operations (for einops)
einops>=0.7.0

# Utilities
tqdm>=4.66.0

# Additional dependencies for UNet models
accelerate>=0.25.0
safetensors>=0.4.0

# For IP-Adapter functionality
xformers>=0.0.22  # Optional but recommended for attention optimization
