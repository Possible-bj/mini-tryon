# Requirements for Simplified Step 3: UNet Models
# This version uses the latest stable versions without complex hacks

# Core ML libraries
torch>=2.0.0
torchvision>=0.15.0

# Latest stable diffusers (no version pinning to avoid conflicts)
diffusers>=0.25.0

# Latest stable transformers
transformers>=4.35.0

# Additional utilities
einops>=0.6.0
accelerate>=0.20.0
safetensors>=0.3.0

# Optional: for better performance
xformers>=0.0.20  # Optional: for attention optimization
